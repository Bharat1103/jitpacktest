package jitpack.script

import java.util

object Main extends App{
  val locations = util.Set.of(
    "Networks.apply.hostname1",
    "Networks.apply.hostname2",
    "Networks.apply.hostname3")

  println("Hello this is just a test")

}
